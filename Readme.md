# Serverless Function Repository

This repository contains a collection of serverless functions built using AWS Lambda. Each function is designed to handle specific tasks and can be deployed individually or as part of a larger system.


### Installation

Clone the repository and install dependencies:

```bash
git clone git@github.com:darshit-sharma-neu/serverless.git
cd serverless
npm install
./create-lambda-package.sh
```
